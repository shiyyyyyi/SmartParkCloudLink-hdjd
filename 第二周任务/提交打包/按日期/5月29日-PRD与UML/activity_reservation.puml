@startuml 车位在线预约-活动图
' ============================================
' 活动图：UC-04 车位在线预约
' 核心流程：搜索车场 → 选车位 → 锁定 → 到场确认
' ============================================

title 车位在线预约流程 (UC-04)

start

:车主登录系统;
:进入车主首页;

:输入目的地搜索车场;

:系统展示周边车场列表
（名称、距离、空余车位、费率）;

if (有可用车场?) then (否)
  :提示"未找到相关停车场";
  stop
else (是)
  :车主选择目标车场;
endif

:查看车场详情与车位列表;

if (有空余车位?) then (否)
  :提示"暂无空余车位";
  stop
else (是)
  :车主选择具体车位;
  :点击"立即预约";
endif

:系统检查车位状态;

if (车位状态=free?) then (否)
  :提示"该车位已被他人锁定"
  建议重新选择;
  detach
else (是)
  :系统锁定车位
  状态变更: free → locked;
  :生成预约记录
  倒计时15分钟开始;
endif

:显示预约成功页面
（车位号、车场名、倒计时）;

fork
  :车主到场;
  :点击"确认到达";
  :系统验证预约有效;
  if (未超时?) then (是)
    :车位状态: locked → occupied;
    :预约状态: active → confirmed;
    :记录入场时间;
    :导航指引车主入场;
    #palegreen:预约完成;
  else (否)
    :提示"预约已超时"
    车位已释放;
    stop
  endif
fork again
  :等待中...;
  :倒计时到0;
  :系统自动取消预约;
  :车位状态: locked → free;
  :通知车主"预约已超时";
  stop
fork again
  :车主主动取消;
  :确认取消;
  :车位状态: locked → free;
  :预约状态: active → cancelled;
  stop
end fork

stop

@enduml
