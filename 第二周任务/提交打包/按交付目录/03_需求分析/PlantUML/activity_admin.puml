@startuml 管理员运营后台-活动图
' ============================================
' 活动图：UC-08/UC-09 管理员运营流程
' 核心流程：登录 → 管理车场 → 查看运营数据
' ============================================

title 管理员运营后台流程 (UC-08/UC-09)

start

:管理员登录系统;

:系统验证角色=admin;

if (验证通过?) then (否)
  :跳转登录页
  提示"无管理权限";
  stop
else (是)
  :进入管理后台首页;
endif

:显示管理后台主界面
┌────────────────────┐
│ 停车场管理          │
│ 车位状态实时监控     │
│ 收入统计            │
│ 车流统计            │
│ 数据看板入口        │
└────────────────────┘;

fork
  :管理停车场信息
  (UC-08);
  
  :查看所辖车场列表;
  
  if (需要新增车场?) then (是)
    :填写车场信息
    （名称、地址、总车位、费率）;
    :系统校验数据;
    if (校验通过?) then (是)
      :保存车场信息;
      :初始化对应车位数;
    else (否)
      :提示具体校验错误;
    endif
  else (否)
    :查看/编辑现有车场;
  endif
  
  :查看车位实时状态
  （空闲/锁定/占用/禁用）;

fork again
  :查看运营数据
  (UC-09);
  
  :选择车场;
  :查看统计数据;
  note right
    - 今日/本周/本月收入
    - 车流统计（进场/出场）
    - 车位占用率
    - 收入趋势
  end note;
  
  if (需要数据看板?) then (是)
    :进入数据看板 (UC-10);
    :ECharts可视化展示;
    note right
      ① 车位周转率折线图
      ② 高峰时段柱状图
      ③ 收入趋势图
      ④ 车场利用率饼图
    end note;
  else (否)
    :返回运营后台;
  endif

end fork

stop

@enduml
